import { Modal } from "antd";
import { useTranslation } from "react-i18next";

export const FullProjectModal = () => {
  const { t } = useTranslation('projects');

  return(
    <Modal>
      
    </Modal>
  );
};